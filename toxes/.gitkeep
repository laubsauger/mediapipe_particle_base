# Keep this folder in git even when MediaPipe.tox (the only large item) is
# untracked. Drop sensor adapter .tox files in here. The MediaPipe one is
# .gitignored (172 MB, not redistributed); see docs/sensor_contract.md.
